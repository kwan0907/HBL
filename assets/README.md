# Assets

`assets/` is for non-code resources that do not belong in `icons/`.

- `branding/` — high-resolution logo/brand source files.

PWA icons and favicons remain under `icons/` because the manifest and HTML reference them directly.

Do not store product price data, JavaScript, or duplicate runtime entry files here.
